import Link from 'next/link';import { randomUUID } from 'node:crypto';import { ActionForm } from '@/components/action-form';import { tenantContext } from '@/lib/access';import { createCustomerRequest } from '../actions';
export const dynamic='force-dynamic';
export default async function NewRequest({params}:{params:Promise<{tenantId:string}>}){const{tenantId:input}=await params;const{db,tenantId}=await tenantContext(input);const{data:devices}=await db.from('equipment').select('id,kind,brand,model,serial_number').eq('tenant_id',tenantId).order('created_at',{ascending:false});return <><Link href={`/cliente/t/${tenantId}`}>← Mis servicios</Link><div className="page-heading"><div><p className="eyebrow">CL03–CL05</p><h1>Nueva solicitud</h1><p className="lead">Describe el equipo y la falla. Enviar la solicitud no confirma que el comercio haya recibido físicamente el equipo.</p></div></div><ActionForm action={createCustomerRequest.bind(null,tenantId,randomUUID())} submit="Crear solicitud" actionId="CL05.A2"><section className="panel"><p className="eyebrow">CL03 · Equipo</p><h2>¿Qué equipo necesita atención?</h2>{devices?.length?<p className="muted">Equipos guardados: {devices.map(d=>[d.kind,d.brand,d.model,d.serial_number].filter(Boolean).join(' ')).join(' · ')}</p>:<p className="muted">Todavía no tienes equipos guardados. Esta solicitud creará la orden sin afirmar recepción.</p>}<label>Equipo, marca y modelo<input name="device" required minLength={2} maxLength={160} placeholder="Ej. Portátil Lenovo IdeaPad 3"/></label></section><section className="panel spaced"><p className="eyebrow">CL04 · Falla</p><h2>Cuéntanos qué sucede</h2><label>Descripción de la falla<textarea name="issue" required minLength={5} maxLength={2000} placeholder="Síntomas, cuándo comenzó y condiciones relevantes"/></label><p className="notice warning">Las fotos y evidencias privadas requieren D09; no se solicitan en este formulario.</p></section><section className="panel spaced"><p className="eyebrow">CL05 · Revisión</p><h2>Confirma tu solicitud</h2><p>El comercio revisará la información y coordinará la recepción. Sólo después de confirmar custodia se emitirá el voucher de recepción.</p></section></ActionForm></>}
